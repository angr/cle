# Read the Docs configuration file
# See https://docs.readthedocs.io/en/stable/config-file/v2.html for details

version: 2
build:
  os: ubuntu-22.04
  tools:
    python: "3.8"

python:
  install:
    - method: pip
      path: git+https://github.com/angr/archinfo.git
    - method: pip
      path: git+https://github.com/angr/pyvex.git
    - method: pip
      path: .
      extra_requirements:
        - docs
